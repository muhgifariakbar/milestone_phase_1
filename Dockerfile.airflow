FROM apache/airflow:2.9.3-python3.11

# No Spark/Java here -- unlike demo-airflow_with_spark, this pipeline is
# pure Python/SQL against PostgreSQL, so the base Airflow image is enough.
USER airflow
COPY requirements.txt requirements-airflow.txt ./
RUN pip install -r requirements.txt -r requirements-airflow.txt
